{
  "name": "dashen-ai-business-consultant-system",
  "version": "1.0.0",
  "private": true,
  "description": "MERN stack AI-powered financial advisory and business consulting platform for Dashen Bank customers.",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace server\" \"npm run dev --workspace client\"",
    "build": "npm run build --workspace client",
    "start": "npm run start --workspace server",
    "seed": "npm run seed --workspace server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
